---
const cards = [
  {
    title: 'Nuestra energía en acción',
    description:
      'Cada paso cuenta una historia. Descubre cómo vivimos la danza día a día en GUDAS.',
    image: 'img1.webp',
  },
  {
    title: 'Pasión por la danza',
    description:
      'La danza es nuestra vida. Aquí, cada movimiento es una expresión de amor y dedicación.',
    image: 'img2.webp',
  },
  {
    title: 'Comunidad vibrante',
    description:
      'Un espacio donde todos somos parte de una gran familia, compartiendo risas y aprendizajes.',
    image: 'img3.webp',
  },
]
---

<div class="col-span-10 bento-item">
  <div class="swiper swiper-container w-full h-full rounded-xl overflow-hidden">
    <div class="swiper-wrapper">
     {
        cards.map((card) => (
          <div
            class="swiper-slide relative flex items-center justify-center min-h-[22rem] md:min-h-[28rem] bg-cover bg-center bg-no-repeat rounded-3xl overflow-hidden"
            style={`background-image: url('/images/about/${card.image}')`}
            role="group"
            aria-label={card.title}
          >
            <!-- Capa de gradiente -->
            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/50 to-transparent z-0 rounded-3xl" />

            <!-- Contenido del slide -->
            <div class="relative z-10 flex flex-col items-center justify-center w-full h-full px-6 py-10 gap-4 text-center">
              <h3 class="text-2xl md:text-3xl font-extrabold text-primary drop-shadow-md">
                {card.title}
              </h3>
              <p class="text-base md:text-lg text-white/90 font-light max-w-md drop-shadow">
                {card.description}
              </p>
              <span class="text-sm md:text-base text-white/70 italic tracking-wide">
                "Vive la experiencia GUDAS"
              </span>
            </div>
          </div>
        ))
      }

      <!-- Si quieres paginación o flechas -->
      <div class="swiper-pagination"></div>
    </div>
  </div>
</div>

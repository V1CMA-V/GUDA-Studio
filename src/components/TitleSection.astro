---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="flex flex-col items-center mb-15 w-fit gap-3">
  <h2 class="text-3xl md:text-5xl font-bold">{title}</h2>
  <div class="w-3/4 h-1 bg-gradient-to-r from-primary to-[#4079ff]"></div>
  <slot />
</div>

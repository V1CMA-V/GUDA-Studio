---
interface Props {
  isBig?: boolean
}

const { isBig } = Astro.props
---

<a
  href="https://wa.me/521234567890"
  target="_blank"
  rel="noopener"
  class={`font-semibold text-white bg-gradient-to-r from-primary to-primary/80 hover:from-primary/90 hover:to-primary hover:scale-105 transition-all duration-200 flex items-center justify-center gap-2 group rounded-full shadow-lg border border-white/20 ${isBig ? 'px-8 py-4 text-2xl min-w-[280px]' : 'px-6 py-3 text-lg min-w-[160px]'}`}
>
  <span>Inscríbete</span>
  <svg
    class={`group-hover:-rotate-12 transition-transform duration-200 ${isBig ? 'w-7 h-7' : 'w-5 h-5'}`}
    fill="none"
    viewBox="0 0 24 24"
    stroke="currentColor"
    stroke-width="2"
    ><path
      d="M5 12h14M13 6l6 6-6 6"
      stroke-linecap="round"
      stroke-linejoin="round"></path></svg
  >
</a>

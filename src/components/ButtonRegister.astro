---
interface Props {
  isBig?: boolean
}

const { isBig } = Astro.props
---

<a
  href="https://wa.me/521234567890"
  target="_blank"
  rel="noopener"
  class={`font-semibold text-white hover:scale-105 transition-transform duration-200 flex items-center gap-2 group ${isBig ? 'px-5 py-2 text-3xl' : 'px-5 py-2 text-lg'}`}
>
  <span>Inscríbete</span>
  <svg
    class="w-6 h-6 group-hover:-rotate-12 transition-transform duration-200"
    width="18"
    height="18"
    fill="none"
    viewBox="0 0 24 24"
    stroke="currentColor"
    ><path
      d="M5 12h14M13 6l6 6-6 6"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"></path></svg
  >
</a>

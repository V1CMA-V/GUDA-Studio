---
import Map from '@/components/Map.astro'
const contactInfo = [
  { label: 'Dirección', value: 'Calle Ejido 123, Atlixco, Puebla, México' },
  { label: 'Teléfono', value: '+52 222 123 4567' },
  { label: 'Email', value: 'contacto@gudasstudio.com' },
]
---

<section
  class="flex flex-col md:flex-row items-center w-full min-h-[60vh] shadow-lg bg-background-cards"
>
  <!-- Contenedor del Mapa -->
  <div
    id="map-container"
    class="flex-1 w-full min-h-[14rem] md:h-full flex items-center justify-center p-10 md:p-8 z-10"
  >
    <Map
      class="w-full h-48 md:h-96 lg:h-[28rem] rounded-2xl overflow-hidden border-2 border-primary/60"
    />
  </div>

  <!-- Información -->
  <div
    class="flex-1 w-full flex flex-col items-center md:items-start justify-center p-4 md:p-12 gap-4"
  >
    <h3
      class="text-3xl md:text-4xl font-extrabold text-primary mb-2 tracking-tight drop-shadow-sm"
    >
      Visítanos
    </h3>
    <p
      class="text-lg md:text-xl max-w-2xl text-center md:text-left text-secondary leading-relaxed"
    >
      Estamos <strong>ubicados en Atlixco, Puebla.</strong> Ven a conocernos y descubre
      todo lo que <strong>Gudas Studio</strong> tiene para ofrecerte.
    </p>
    {
      contactInfo.map(({ label, value }) => (
        <p class="text-lg md:text-xl max-w-2xl text-center md:text-left text-secondary leading-relaxed">
          <strong>{label}:</strong> {value}
        </p>
      ))
    }

    <a
      href="https://goo.gl/maps/xyz123"
      target="_blank"
      rel="noopener noreferrer"
      class="google-link text-lg md:text-xl text-primary transition-colors duration-200"
      >Ver en Google Maps</a
    >
  </div>
</section>

<style>
  h2,
  h3,
  p,
  #map-container {
    animation: fadeIn linear both;
    animation-timeline: view();
    animation-range: entry 15% cover 30%;
  }
  .google-link {
    position: relative;
    display: inline-block;
    text-decoration: none;
    cursor: pointer;
  }
  .google-link::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -2px;
    width: 0;
    height: 2px;
    background: currentColor;
    transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .google-link:hover::after {
    width: 100%;
  }
</style>
